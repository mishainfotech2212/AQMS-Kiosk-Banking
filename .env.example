# Required — copy from Supabase Dashboard → Project Settings → API → anon public key
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Optional — defaults to https://api.caribbargains.com/queueflow-api/functions/v1
# EXPO_PUBLIC_API_BASE_URL=https://api.caribbargains.com/queueflow-api/functions/v1

# After editing .env, restart Metro with a clean cache:
# npx expo start -c
